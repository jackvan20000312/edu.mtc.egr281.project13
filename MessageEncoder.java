package project13;

public interface MessageEncoder{
	// encode and returns the given plain text
	public abstract String encode(String plainText);
}// Ending bracket of interface MessageEncoder
